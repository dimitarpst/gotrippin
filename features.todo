Todo:
  ☐ AI operator — Phase 3b: Dock as collapse bubble (compact on side, expand left/right); keep off AI page
  ☐ AI operator — Phase 5: Collaboration features and advanced planning behaviours (multi-user chat, locks, refinements)
  ☐ Teacher feedback — more activities and notes
  ☐ Explore another weather api cuz current one sucks - look at https://open-meteo.com/en/docs

Supabase Security:
  ☐ Password changed — gotrippin-branded HTML + enable (Auth → Emails → Security)
  ☐ Email address changed — template + enable
  ☐ Phone number changed — template + enable
  ☐ Identity linked — template + enable
  ☐ Identity unlinked — template + enable
  ☐ Multi-factor authentication method added — template + enable
  ☐ Multi-factor authentication method removed — template + enable

To plan:
  ☐ How to display the activities on the map - together with the route? or seperate?
  ☐ How to display the weather on the map?

Trip UX (Tripsy-inspired — tight density, map + sheets):
  ✔ Phase 1 — Trip hub horizontal action cluster (+ Activity, Flight, Lodging/Place where routes exist); replace isolated large FAB and trim vertical padding around it (`trip-overview.tsx`)
  ✔ Phase 2 — Overview density tokens: tighter card padding, section gaps, list spacing (same file; optional small shared pattern for cards on this page)
  ✔ Phase 3 — Itinerary preview rows: shallower rows, typography hierarchy, right-aligned meta where it fits, optional hairline dividers between stops (`trip-overview.tsx`)
  ✔ Phase 4 — Route map bottom sheet: compact bottom bar (menu / more / primary +) and sheet content using same density rules (`RouteMapPageClient.tsx` + shared `Drawer` patterns)
  ✔ Phase 5 — Timeline and per-day location views: reuse the same spacing scale (`app/trips/[id]/timeline/**`)
  ✔ Later — Expenses & documents cards (when features exist); match tight card + list pattern for consistency — budget: full page `/trips/[id]/budget` + overview row card below notes; documents card unchanged
  ☐ Tripsy day-first flow (ref. screenshots — Vilnius hub card, white day list, TapSmart carousel): horizontal day strip scoped to trip window; selected day drives the list below; rows = primary title left + time/meta right in muted type (not a fat Mar→Mar span per stop on compact UI); hub itinerary card = one trip date range on the header row (small grey, right) plus optional summary row (activity-type icons + count like “4 activities”); keep multi-night / arrival–departure truth on timeline & stop detail, not repeated on overview

Archive:
  ✔ (from Todo, 2026-04-02) Teacher feedback — when changing the main date, update the date under it as well — shift route + activity datetimes by start delta; trip overview date picker dots + long-press detail toast
  ✔ (from Todo, 2026-04-02) Teacher feedback — trip photos should be uploadable by the person — cover: device upload to R2 (`trip-images/uploads/{userId}/…`), `photos.source=upload`, create/edit trip + background picker @done(26-03-31)
  ✔ (from Todo, 2026-04-02) Teacher feedback — export trip as PDF — trip overview menu → `/trips/[id]/print` (site Tailwind + browser Save as PDF) @done(26-03-31)
  ✘ Teacher feedback — button to extend the last route @cancelled(26-03-31 21:06)
  ✔ (from Todo, 2026-04-02) Teacher feedback — AI-readable support (site can be read by AI) — `public/llms.txt`, `app/robots.ts` @done(26-03-31)
  ✔ (from Todo, 2026-04-02) Teacher feedback — change home color palette — light/dark via `next-themes` + shadcn tokens; coral primary; landing + trips; trip overview cards/menus use semantic fg/bg (no forced `.dark` on `/trips`) @done(26-03-31)
  ✔ (from Todo, 2026-04-02) Investigate url of background photo fked up while editing trip — normalize R2 keys in `getR2PublicUrl` (strip leading `/`, pass through absolute URLs) @done(26-03-31)
  ✔ shadcn theme alignment — native variable semantics, custom colors only (no overwrites)
    ✔ Phase 1 — globals.css: single :root, shadcn semantics, add --surface-alt
    ✔ Phase 2 — tailwind.config.js: align theme colors to CSS (oklch/vars)
    ✔ Phase 3 — Fix LinkedAccountsCard bg-(--accent) typo
    ✔ Phase 4 — Replace muted text (var(--muted)) → text-muted-foreground across all files
  ✔ Maps integration — Mapbox + coordinates (docs/MAPS_IMPLEMENTATION.md)
  ✔ Route screen — map-first layout with bottom sheet editor (docs/MAPS_IMPLEMENTATION.md, docs/route-based-trip-planning-architecture.md)
    ✔ Phase 1 — Map + bottom sheet scaffolding (read-only route; no editing)
    ✔ Phase 2 — Edit existing stops in sheet (reorder, rename, dates) synced to map
    ✔ Phase 3 — Add stops via search (Google Places)
    ✔ Phase 3b — Add stops via map click
    ✘ Phase 4 — Draggable markers, extra map polish, animations (moved to later backlog / nice-to-have)
    ✔ Phase 4a — Basemap: switch to Mapbox Standard (or streets) for POI/label richness (Option 1)
    ✘ Phase 4b — Basemap style toggle (e.g. Night ↔ Streets ↔ Satellite) — move to later backlog
    ✘ Phase 4c — Nearby POIs overlay (cut after experiment; keep text search + dropped pins for now)
    ✔ Phase 4d — Along-route POIs helper: sample route from waypoints, query nearby per segment midpoint, dedupe + small list in drawer
    ✘ Phase 4e — Unify map + edit mode: keep Route page as primary; map page redirects/aliases (defer for now)
    ✔ Phase 5 — Align Create Trip route step with map-first route editor (shared RouteEditor UX) — create wizard now: Step 1 (details) → Step 2 (map route editor at /trips/{share_code}/route)
  ✔ Map UX research — dynamic markers, popups, clicks & draggable markers via react-map-gl / Mapbox docs (Context7)
  ✔ AI operator — glm-5 via OpenRouter (docs/AI_TRIP_PLANNER.md, docs/plan.md, docs/AI_OPERATOR_IMPLEMENTATION.md) @done(26-03-31 14:24)
    ✔ Phase 1 — AI backend foundations (DB session model, tools catalog, OpenRouter client)
    ✔ Phase 2 — Turn orchestration & tool execution (single- and multi-tool calls) in NestJS ai module
    ✔ Phase 3 — Web chat UI (global + trip-scoped) wired to AI endpoints, with basic cards/quick replies
    ✔ Phase 3a — AI chat UX: markdown rendering, loading animation, design aligned to project
    ✔ Phase 3c — Edit message (draft before send and/or edit last message to re-send)
    ✔ Phase 4 — Logging, metrics, rate limits and error handling for AI calls
  See docs/todos/later.md for Phase 6 backlog.
  See docs/todos/archive.md for full history.
